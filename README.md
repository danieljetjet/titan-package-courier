# Package courier

A drop of third-party NuGet packages (`.nupkg`), and nothing else. No source, no project files, no
configuration, no documentation. Every file here is a public artifact already published on nuget.org.

Consume it by adding `packages/` as a NuGet source.
